rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /exam-pdfs/{file} {
      allow read: if request.auth != null;
      allow write: if request.auth != null &&
                     request.auth.token.email == 'sean.sirota.2002.09@gmail.com';
    }
  }
}
