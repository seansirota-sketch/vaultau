# Dependencies
node_modules/

# Netlify internal folder (this is the one we talked about!)
.netlify/

# Environment variables (secrets)
.env

# Firebase
.firebase/
firebase-debug.log
ui-debug.log
firestore-debug.log
storage-debug.log
emulator-data/
firebase-export-*/

# Local emulator rules (open rules, emulator only — never deploy)
*.rules.local

# OS generated files (Mac/Windows hidden files)
.DS_Store
Thumbs.db